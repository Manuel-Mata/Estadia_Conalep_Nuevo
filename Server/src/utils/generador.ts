export function generarReferenciaPago(referenciaBase: string, fecha: string, importe: number, variable: string): string {
  // Aquí va tu función que convierte todo a referencia con verificador
  return referenciaBase + "0000" + "0" + variable + "99"; // Temporal
}
